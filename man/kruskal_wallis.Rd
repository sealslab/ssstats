% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kruskal_wallis.R
\name{kruskal_wallis}
\alias{kruskal_wallis}
\title{Kruskal-Wallis H Test}
\usage{
kruskal_wallis(data, outcome, A, alpha = 0.05)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{outcome}{Unquoted column name for the continuous outcome (numeric).}

\item{A}{Unquoted column name for the grouping variable. Must have at
least 2 levels.}

\item{alpha}{Numeric. Significance level between 0 and 1 (exclusive).
Defaults to `0.05`.}
}
\value{
Called for its side effect (printed output); returns `NULL`
  invisibly.
}
\description{
Performs a Kruskal-Wallis H test for differences in location
  across two or more independent groups. Prints a formatted summary
  including per-group medians and sample sizes, hypotheses, the H test
  statistic with degrees of freedom, p-value, and conclusion.

  The H statistic is referred to a chi-squared distribution with k - 1
  degrees of freedom, where k is the number of groups. Ties are handled
  automatically via the standard correction applied by
  \code{\link[stats]{kruskal.test}}.
}
