% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_squared.R
\name{r_squared}
\alias{r_squared}
\title{Adjusted R-Squared}
\usage{
r_squared(model = NULL, data = NULL, outcome = NULL, function_of = NULL)
}
\arguments{
\item{model}{A fitted model object of class \code{lm}, \code{glm} (identity
link only), or \code{aov}. If \code{NULL}, \code{data}, \code{outcome},
and \code{function_of} must be supplied.}

\item{data}{A data frame or tibble. Used only when \code{model} is
\code{NULL}.}

\item{outcome}{Unquoted column name for the outcome variable. Used only when
\code{model} is \code{NULL}.}

\item{function_of}{Unquoted expression for the right-hand side of the model
formula. Used only when \code{model} is \code{NULL}.}
}
\value{
A numeric scalar: the adjusted R-squared, rounded to 4 decimal
  places.
}
\description{
Computes the adjusted R-squared from a fitted model object or
  directly from data and variable names.
}
