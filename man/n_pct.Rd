% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_functions.R
\name{n_pct}
\alias{n_pct}
\title{Compute Counts and Percentages for One- or Two-Way Tables}
\usage{
n_pct(
  data,
  row_var,
  col_var = NULL,
  rows = NULL,
  digits = 1,
  denom = "col",
  default_rows = 10,
  require_rows_if_truncated = TRUE
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{row_var}{A column name (unquoted) for the row variable.}

\item{col_var}{Optional column name (unquoted) for the column variable. If NULL, a one-way table is returned.}

\item{rows}{Number of rows to print (default is 3).}

\item{digits}{Number of decimal places for percentages (default is 1).}

\item{denom}{Denominator for percentage calculation in two-way tables; one of "row", "col", or "all" (default is "col").}
}
\value{
A tibble with counts and percentages formatted as "n (pct)".
}
\description{
Compute Counts and Percentages for One- or Two-Way Tables
}
\examples{
\dontrun{
n_pct(mtcars, cyl)
n_pct(mtcars, cyl, gear)
}
}
