% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_pct.R
\name{n_pct}
\alias{n_pct}
\title{Frequency Table with Counts and Percentages}
\usage{
n_pct(data, row, col = NULL, show = 10, digits = 1, denom = "col")
}
\arguments{
\item{data}{A data frame or tibble.}

\item{show}{Maximum number of rows to return (or show). Defaults to 
`10`. Set to `Inf` to return all rows.}

\item{digits}{Number of decimal places for percentages. Defaults to `1`.}

\item{denom}{Denominator for percentages in two-way tables: `"col"`
(default), `"row"`, or `"all"`.}

\item{row_var}{Unquoted column name for the row variable.}

\item{col_var}{Unquoted column name for the column variable. If `NULL`
(default), a one-way table is returned.}
}
\value{
A tibble with one row per level of `row_var` and an `n (pct)`
  column for each level of `col_var` (or a single `n (pct)` column for
  one-way tables).
}
\description{
Produces a one- or two-way frequency table with counts and
  percentages combined into "n (pct)" cells. When the result is truncated
  to `show`, a message is emitted.
}
