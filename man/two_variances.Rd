% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_variances.R
\name{two_variances}
\alias{two_variances}
\title{Two-Sample F-Test for Equality of Variances}
\usage{
two_variances(
  data,
  outcome,
  A,
  first = NULL,
  alternative = "two.sided",
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{outcome}{Unquoted column name for the continuous outcome (numeric).}

\item{A}{Unquoted column name for the grouping variable. Must have exactly
two levels.}

\item{first}{Character string. The group level to treat as the first term
in the ratio (i.e., group1 in \eqn{\sigma^2_{group1} / \sigma^2_{group2}}).
Defaults to \code{NULL}, in which case groups are ordered alphabetically.}

\item{alternative}{Character string specifying the alternative hypothesis.
One of \code{"two.sided"} (default), \code{"two"}, \code{"less"}, or
\code{"greater"}. The confidence interval is always two-sided regardless
of this setting.}

\item{alpha}{Numeric. Significance level between 0 and 1 (exclusive).
Defaults to \code{0.05}. The confidence interval is computed at
\code{(1 - alpha) * 100\%}.}
}
\value{
Called for its side effect (printed output); returns \code{NULL}
  invisibly.
}
\description{
Performs a two-sample F-test for equality of variances. Prints
  a formatted summary including per-group sample variances and standard
  deviations, the variance ratio point estimate, a two-sided confidence
  interval for \eqn{\sigma^2_1 / \sigma^2_2}, hypotheses, F test statistic,
  p-value, and conclusion.
}
