% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_median.R
\name{one_median}
\alias{one_median}
\title{One-Sample Median (Sign Test)}
\usage{
one_median(data, outcome, m = 0, alternative = "two.sided", alpha = 0.05)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{outcome}{Unquoted column name for the continuous outcome (numeric).}

\item{m}{Numeric. Hypothesized median under the null (default = 0).}

\item{alternative}{Character string specifying the alternative hypothesis.
One of `"two.sided"` (default), `"two"`, `"less"`, or `"greater"`. The
confidence interval is always two-sided regardless of this setting.}

\item{alpha}{Numeric. Significance level between 0 and 1 (exclusive).
Defaults to `0.05`. The confidence interval is computed at
\code{(1 - alpha) * 100\%}.}
}
\value{
Called for its side effect (printed output); returns `NULL`
  invisibly.
}
\description{
Performs a one-sample sign test for the median. Prints a
  formatted summary including the sample median, a two-sided confidence
  interval (via linear interpolation), hypotheses, test statistic,
  p-value, and conclusion.
}
