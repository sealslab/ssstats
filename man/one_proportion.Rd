% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_proportion.R
\name{one_proportion}
\alias{one_proportion}
\title{One-Sample Proportion}
\usage{
one_proportion(
  data,
  outcome,
  event,
  p,
  alternative = "two.sided",
  alpha = 0.05
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{outcome}{Unquoted column name for the binary outcome variable.}

\item{event}{The value of `outcome` to treat as a success.}

\item{p}{Numeric. Hypothesised proportion under the null. Must be between
0 and 1 (exclusive).}

\item{alternative}{Character string specifying the alternative hypothesis.
One of `"two.sided"` (default), `"two"`, `"less"`, or `"greater"`. The
confidence interval is always two-sided regardless of this setting.}

\item{alpha}{Numeric. Significance level between 0 and 1 (exclusive).
Defaults to `0.05`. The confidence interval is computed at
`(1 - alpha) * 100 pct`.}
}
\value{
Called for its side effect (printed output); returns `NULL`
  invisibly.
}
\description{
Performs a one-sample proportion analysis. Prints a formatted
  summary including the sample proportion and count, a two-sided Wald
  confidence interval, hypotheses, z-test statistic, p-value, and
  conclusion.
}
